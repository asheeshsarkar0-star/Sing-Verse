// SingVerse Global JavaScript console.log('SingVerse JS Loaded');

// Smooth scroll function function smoothScroll(target) { document.querySelector(target).scrollIntoView({ behavior: 'smooth' }); }

// Example function for future use function showAlert(msg) { alert(msg); }
